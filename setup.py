#!/usr/bin/env python

import setuptools  # type:ignore

setuptools.setup(
    name="gdist",
    version="0",
    maintainer="Felix Pultar",
    python_requires=">=3.8",
    install_requires=[],
    packages=["gdist"],
)
